//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

final class {{ module_info.name }}Configurator {
    
    func configure() {
        guard let view = UIStoryboard.instantiate(ofType: {{ module_info.name }}ViewController.self) else {
            fatalError("Can't load viewController from storyboard")
        }

        let router = {{ module_info.name }}RouterImplementation(with: view)
        let presenter = {{ module_info.name }}Presenter(with: view)
        view.presenter = presenter
    }  
}
